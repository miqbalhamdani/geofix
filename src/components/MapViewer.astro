<div class="lg:col-span-2 bg-surface-container-lowest border border-outline-variant rounded-lg overflow-hidden min-h-[400px] relative">
  <div id="map" class="absolute inset-0"></div>
  <div class="absolute top-4 right-4 flex flex-col gap-2 z-[1000]">
    <button id="zoom-in-btn" class="bg-white p-2 border border-outline-variant rounded shadow-sm hover:bg-surface-container transition-colors" aria-label="Zoom in">
      <span class="material-symbols-outlined">zoom_in</span>
    </button>
    <button id="zoom-out-btn" class="bg-white p-2 border border-outline-variant rounded shadow-sm hover:bg-surface-container transition-colors" aria-label="Zoom out">
      <span class="material-symbols-outlined">zoom_out</span>
    </button>
  </div>
  <div class="absolute bottom-4 left-4 z-[1000] bg-white px-3 py-1 border border-outline-variant rounded-full text-[10px] font-mono-label">
    EPSG:4326 | ZOOM AUTO
  </div>
</div>
